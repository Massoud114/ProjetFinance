<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Bienvenue!{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        {% if app.user %}
            <h4><a href="{{ path('app_logout') }}">Déconnexion</a></h4>
        {% else %}
            <h4><a href="{{ path('app_login') }}">Se connecter</a></h4>
            <h4><a href="{{ path('app_register') }}">S'inscrire</a></h4>
        {% endif %}
        {% block body %}{% endblock %}
        {% block javascripts %}{% endblock %}
    </body>
</html>
