<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Inscription</title>
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <link rel="stylesheet" href="{{ asset('vendor/bootstrap/css/bootstrap.min.css') }}">
    <link rel="stylesheet" href="{{ asset('vendor/font-awesome/css/font-awesome.min.css') }}">
    <link rel="stylesheet" href="{{ asset('assets/css/site.min.css') }}">
</head>

<body>
    <div class="auth-main">
        <div class="auth_div">
            <div class="card">
                <div class="auth_brand">
                    <a class="navbar-brand" href="javascript:void(0);">Gestion de finances</a>
                </div>
                <div class="body">
                    <p class="lead mb-4">Créer un compte</p>
                    {{ form_start(registrationForm, {
                        attr : {
                            'class' : 'form-auth-small m-t-20 "'
                        }
                    }) }}
                        <div class="form-group">
                            {{ form_row(registrationForm.username, {
                                label: false,
                                attr : {
                                    'class' : 'form-control round',
                                    'placeholder' : 'Nom d\'utilisateur'
                                }
                            }) }}
                        </div>
                        <div class="form-group">
                            {{ form_row(registrationForm.plainPassword.first, {
                                label: false,
                                attr : {
                                    'class' : 'form-control round',
                                    'placeholder' : 'Mot de passe'
                                }
                            }) }}
                        </div>
                        <div class="form-group">
                            {{ form_row(registrationForm.plainPassword.second, {
                                label: false,
                                attr : {
                                    'class' : 'form-control round',
                                    'placeholder' : 'Confirmer le Mot de passe'
                                }
                            }) }}
                        </div>
                        <button type="submit" class="btn btn-primary btn-round btn-block">S'inscrire</button>
                    {{ form_end(registrationForm) }}
                </div>
            </div>
        </div>
    </div>

    <!-- Latest jQuery -->
    <script src="{{ asset('vendor/jquery/jquery-3.3.1.min.js') }}"></script>

    <!-- Bootstrap 4x JS  -->
    <script src=".{{ asset('vendor/bootstrap/js/bootstrap.bundle.min.js') }}"></script>
    <!-- Project Common JS -->
    <script src="{{ asset('assets/js/common.js') }}"></script>
    <script src="{{ asset('assets/js/index.js') }}"></script>
</body>
</html>
